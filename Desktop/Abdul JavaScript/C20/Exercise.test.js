const Exercise = (',/Exercise')

test('proper ttest if it work perfectly', () => {
    expect(sum(1,2).toBe (3) )
})

const Exercise = (',/sum')

test('properskjxm', () =>{
    expect(sum(1,2) . toBe(3))
})